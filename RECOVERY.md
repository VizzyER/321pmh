# 断点续跑数据恢复

## 章节文件缺失但连续性状态存在

如果 `run` 发现 `chapter_NNNN.md` 缺失，而 `novel_state.json` 的
`chapter_summaries` 或 `timeline_events` 已有该章的 `第N章：` 记录，
程序会在调用模型、写入章节或修改状态前中止。自动重生成会重复累积摘要、
时间线及角色变化，因此程序不会自行删除或覆盖这些状态。

恢复前先备份 `novel_state.json` 和 `novel_output`。首选从可信备份恢复对应的
章节 Markdown；若确需重新生成，须人工核对并完整回退该章留下的摘要、时间线
和角色状态后再运行。角色变化不是按章节单独存储的，无法确认安全回退时应恢复
已知良好的完整状态备份，而不是猜测或拼补内容。
